appId: com.videodlp.app
productName: VideoDLP
directories:
  buildResources: resources
  output: dist
mac:
  target:
    - dmg
    - zip
  artifactName: 'VideoDLP-${version}-mac-${arch}.${ext}'
  icon: resources/icon.icns
dmg:
  title: 'VideoDLP ${version}'
files:
  - out/**/*
  - package.json
